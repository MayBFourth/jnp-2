services:
  db:
    image: mysql:8.1.0
    hostname: "mysql"
    ports:
      - "3307:3306"
    environment:
      MYSQL_DATABASE: jnp
      MYSQL_USER: dbuser
      MYSQL_PASSWORD: dbpass
      MYSQL_ROOT_PASSWORD: rootpass